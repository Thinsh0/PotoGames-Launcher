# Plan: Update Helios Launcher to Version 2.4.0

The objective is to bump the application version to 2.4.0 to trigger and test the auto-update mechanism. No other changes are to be made.

## Tasks
- [ ] Update `package.json` version from `2.3.0` to `2.4.0`.
- [ ] Git commit the version bump.
- [ ] Run build command to generate release artifacts.
- [ ] Update documentation artifacts (`plan.md`, `context.md`, `walkthrough.md`).
