# Plan: Update Helios Launcher to Version 2.4.0

The objective is to bump the application version to 2.4.0 to trigger and test the auto-update mechanism. No other changes are to be made.

## Tasks
- [x] Update `package.json` version from `2.3.0` to `2.4.0`.
- [x] Git commit the version bump.
- [x] Run build command to generate release artifacts. (Local build successful)
- [x] Fix GitHub Actions failure (redundant cross-platform flags in `dist` script).
- [ ] Trigger GitHub Release again.
- [x] Update documentation artifacts (`plan.md`, `context.md`, `walkthrough.md`).

## Log (Moved to walkthrough.md)
